---
interface Props {
  wide?: boolean;
}

const { wide = false } = Astro.props;
---

<div class:list={["container", wide && "wide"]}>
  <slot />
</div>

<style>
  .container {
    margin: auto;
    padding: 1em;
    max-width: 80ch;

    &.wide {
      max-width: 130ch;
    }
  }
</style>
